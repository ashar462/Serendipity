/* Header — placeholder until Phase 1 build-out against exact Figma geometry.
   (Nav links & logo positions will be hard-coded from the JSON bboxes.) */
export default function Header() {
  return (
    <header style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: 100, zIndex: 50 }}>
      <p style={{ padding: '20px 60px' }} className="t-body-19">Header — Phase 1</p>
    </header>
  )
}
